# Python bytecode / caches
__pycache__/
*.py[cod]

# Virtual environments
venv/
.venv/
env/

# OS cruft
.DS_Store

# Local tooling / editor config
.claude/

# Runtime + test outputs (masked KYC images, debug renders)
sample-docs/
sample/
sample-specifics/

# Liveness model cache for local dev (LIVENESS_MODELS_DIR=./liveness_models).
# ~330 MB of weights, fetched at runtime — never committed.
liveness_models/

# Backups & logs
*.bak
*.log

# Environment / secrets
.env
.env.*
!.env.example
