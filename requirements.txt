# Core
numpy==2.2.6
Pillow==10.4.0
# 4.11 returns cv2.HoughLinesP as (N,4); estimate_skew_angle unpacks lines[:,0]
# assuming (N,1,4) and 500s on the OCR path. 4.13 restores (N,1,4). See the
# July-2026 rollback: the model-only /healthz let the broken build deploy green.
opencv-python-headless==4.13.0.92
pypdfium2==4.30.0
streamlit==1.48.1

# HTTP API
fastapi==0.118.0
uvicorn[standard]==0.32.1
python-multipart==0.0.20

# Primary OCR — PaddleOCR (PP-OCR) models served via ONNX Runtime.
# No PaddlePaddle, no GPU needed; the ONNX session releases the GIL so
# asyncio.gather over crops runs concurrently.
rapidocr-onnxruntime==1.4.4
onnxruntime==1.22.1

# Fallback OCR — invoked only on individual low-confidence crops.
surya-ocr==0.17.0
torch==2.8.0
torchvision==0.23.0

# Utils
scipy
pandas
